# ===== Server Configuration =====
NODE_ENV=development
PORT=3000
APP_BASE_URL=http://localhost:3000

# ===== Database (PostgreSQL) =====
# ⚠️ هذه هي كلمة المرور التي أرسلتها سابقًا في المحادثة نصيًا. إن كانت حقيقية (غير
# مجرد مثال) فهي مكشوفة الآن ويجب اعتبارها غير آمنة، وقد شرحت سابقًا كيف تُعيد تعيينها
# من Supabase Dashboard. إذا أعدت تعيينها، عدّل هذا السطر بكلمة المرور الجديدة (مع نفس
# الترميز أدناه للرموز الخاصة: $ → %24, , → %2C, # → %23).
DATABASE_URL=postgresql://postgres:KR6-p%24r3Zp%2CN%23fD@db.qopoabyqsipwzejaqiga.supabase.co:5432/postgres

# ===== Redis =====
REDIS_URL=redis://localhost:6379

# ===== CORS =====
CORS_ORIGIN=http://localhost:5173,http://localhost:3001,http://localhost:8080

# ===== Logging (اختياري) =====
LOG_LEVEL=debug
