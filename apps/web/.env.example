# رابط الخادم الخلفي (NestJS) بعد نشره على Render أو أي استضافة أخرى تدعم WebSocket دائم.
# Netlify لا يستطيع استضافة هذا الخادم بنفسه — راجع apps/server/README (قسم النشر).
VITE_API_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
